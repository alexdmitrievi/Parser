{
  "name": "tender-parser",
  "private": true,
  "description": "Python/FastAPI — npm-скрипты только для удобного локального запуска",
  "scripts": {
    "dev": "python -m uvicorn api.main:app --reload --host 127.0.0.1 --port 8000"
  }
}
